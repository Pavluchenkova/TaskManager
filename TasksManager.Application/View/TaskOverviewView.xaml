<Page x:Class="TasksManager.Application.View.TaskOverviewView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TasksManager.Application.View"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="TaskOverviewView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="0"
                  Margin="5"
                  Name="TaskListView"
                  SelectionChanged="TaskListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Label Grid.Row="0" Content="{Binding Title}" FontWeight="Bold"></Label>
                            <Label Grid.Row="1" Content="{Binding Status}" FontWeight="Normal"></Label>
                            <Label Grid.Row="2" Content="{Binding Priority}" FontWeight="Normal"></Label>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
            </Grid.RowDefinitions>
            
            <Label Grid.Row="0" Content="Title"></Label>
            <Label Grid.Row="1" Content="Status"></Label>
            <Label Grid.Row="2" Content=" Priority"></Label>
            <Label Grid.Row="3" Content="Category"></Label>
            <Label Grid.Row="4" Content="Description"></Label>
            <Label Grid.Row="5" Content="CreationDate"></Label>
            
            <Label Grid.Row="0" Grid.Column = "1" Content="{Binding Title}"></Label>
            <Label Grid.Row="1" Grid.Column = "1" Content="{Binding Status}"></Label>
            <Label Grid.Row="2" Grid.Column = "1" Content="{Binding Priority}"></Label>
            <Label Grid.Row="3" Grid.Column = "1" Content="{Binding Category}"></Label>
            <Label Grid.Row="4" Grid.Column = "1" Content="{Binding Description}"></Label>
            <Label Grid.Row="5" Grid.Column = "1" Content="{Binding CreationDate}"></Label>   
            
        </Grid>
    </Grid>
</Page>
