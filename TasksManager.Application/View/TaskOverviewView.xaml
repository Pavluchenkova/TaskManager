<Window x:Class="TasksManager.Application.View.TaskOverviewView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewmodel="clr-namespace:TasksManager.Application.ViewModel"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:TasksManager.Application.TemplateSelector"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="TaskOverviewView" Height="600" Width="800"
        DataContext="{Binding Source={StaticResource mainViewModelLocator}, Path=TaskOverviewViewModel}">

    <Window.Resources>
        <local:TemplateSelector x:Key="TemplateSelector"/>

        <Style x:Key="ShowHideButton" TargetType="{x:Type Button}">
            <Setter Property="Visibility" Value="Hidden"/>
            <Style.Triggers>                
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=IsMouseOver}" Value="true">
                    <Setter Property="Visibility" Value="visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <DataTemplate x:Key="TaskDataTemplate">
            <Border BorderThickness="1">
                <DockPanel HorizontalAlignment="Stretch">
                    <Label HorizontalAlignment="Left" Content="{Binding Title}" FontWeight="Bold" FontSize="16"></Label>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button HorizontalAlignment="Right" Style="{StaticResource ShowHideButton}" ToolTip="Done" DataContext="{Binding DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType=ListView}}">
                            <materialDesign:PackIcon Kind="Check"  Height="24" Width="24" />                            
                        </Button>
                        <Button HorizontalAlignment="Right" Style="{StaticResource ShowHideButton}" ToolTip="Pensil" Command="{Binding DetailCommand}" DataContext="{Binding DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType=ListView}}">
                            <materialDesign:PackIcon Kind="Pencil"  Height="24" Width="24" />
                        </Button>
                        <Button HorizontalAlignment="Right" Style="{StaticResource ShowHideButton}" ToolTip="Delete" Command="{Binding DeleteTaskCommand}" DataContext="{Binding DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType=ListView}}">
                            <materialDesign:PackIcon Kind="Delete"  Height="24" Width="24" />
                        </Button>
                    </StackPanel>
                </DockPanel>
            </Border>
        </DataTemplate>
        
        <DataTemplate x:Key="EditTaskDataTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="60"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column ="0"  Content="Title"></Label>
                <Label Grid.Row="1" Grid.Column ="0"  Content="Status"></Label>
                <Label Grid.Row="2" Grid.Column ="0"  Content=" Priority"></Label>
                <Label Grid.Row="3" Grid.Column ="0"  Content="Category"></Label>
                <Label Grid.Row="4" Grid.Column ="0"  Content="Description"></Label>
                <Label Grid.Row="5" Grid.Column ="0"   Content="CreationDate"></Label>

                <TextBox Grid.Row="0" Grid.Column = "1" ></TextBox>
                <TextBox Grid.Row="1" Grid.Column = "1" ></TextBox>
                <TextBox Grid.Row="2" Grid.Column = "1" ></TextBox>
                <TextBox Grid.Row="3" Grid.Column = "1" ></TextBox>
                <TextBox Grid.Row="4" Grid.Column = "1" ></TextBox>
                <TextBox Grid.Row="5" Grid.Column = "1" ></TextBox>
                <Button Grid.Row="6" Grid.Column="1"
                        HorizontalAlignment="Right"
                        Margin="8"
                        ToolTip="Save"
                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                    <materialDesign:PackIcon Kind="Check"/>
                </Button>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <materialDesign:ColorZone Padding="16"
                                  Margin="0"
                                  materialDesign:ShadowAssist.ShadowDepth="Depth2"
                                  Mode="PrimaryMid"
                                  DockPanel.Dock="Top">
            <DockPanel>
                <ToggleButton DockPanel.Dock="Left" Style="{DynamicResource MaterialDesignHamburgerToggleButton}" />
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20">Task Manager</TextBlock>
            </DockPanel>
        </materialDesign:ColorZone>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="26"></RowDefinition>
                <RowDefinition/>
                <RowDefinition Height="250"/>
            </Grid.RowDefinitions>
            <materialDesign:ColorZone Grid.Column="0" Grid.Row="0" Margin="0" materialDesign:ShadowAssist.ShadowDepth="Depth1" Mode="PrimaryLight">
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16">To Do:</TextBlock>
            </materialDesign:ColorZone>
            <ListView Grid.Column="0" 
                      Grid.Row ="1"
                      Margin="0"
                      Name="TaskListView"
                      HorizontalContentAlignment="Stretch"
                      ItemsSource="{Binding Tasks}"
                      SelectedItem="{Binding SelectedTask}">
                <ListView.ItemTemplateSelector>
                    <local:TemplateSelector
                          EditTaskDataTemplate = "{StaticResource EditTaskDataTemplate}"
                          TaskDataTemplate = "{StaticResource TaskDataTemplate}"/>
                </ListView.ItemTemplateSelector>
            </ListView>
            <!--<Button Grid.Row="2" Grid.Column="0" Command="{Binding DetailCommand}" Margin="50 50 50 50">Detail</Button>-->

            <StackPanel Grid.Row="2" Height="200">
                <Button VerticalAlignment="Center"
                        Margin="5"
                        Command="{Binding DetailCommand}"
                        Content="detail/edit"  />
                <Button Style="{StaticResource MaterialDesignFloatingActionButton}"
                        Margin="5" 
                        HorizontalAlignment="Right" 
                        ToolTip="Add task"
                        Command="{Binding AddTaskCommand}">
                    <materialDesign:PackIcon Kind="Plus"  Height="24" Width="24" />
                </Button>
                <Button Style="{StaticResource MaterialDesignFloatingActionButton}"
                        Margin="5" 
                        HorizontalAlignment="Right" 
                        ToolTip="Delete task"
                        Command="{Binding DeleteTaskCommand}">
                    <materialDesign:PackIcon Kind="Delete"  Height="24" Width="24" />
                </Button>

            </StackPanel>
            <materialDesign:ColorZone Grid.Column="1" Grid.Row="0" Margin="0" materialDesign:ShadowAssist.ShadowDepth="Depth2" Mode="PrimaryLight" Height="19">
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16">In Progress</TextBlock>
            </materialDesign:ColorZone>
            <ListView Grid.Column="1"
                      Grid.Row="1"
                      Margin="0"
                      ItemsSource="{Binding TasksInProcess}"
                      Name="TaskInProgressListView"
                      SelectedItem="{Binding SelectedTask}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"></RowDefinition>
                                    <RowDefinition Height="*"></RowDefinition>
                                    <RowDefinition Height="*"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Label Grid.Row="0" Content="{Binding Title}" FontWeight="Bold"></Label>
                                <Label Grid.Row="1" Content="{Binding Status}" FontWeight="Normal"></Label>
                                <Label Grid.Row="2" Content="{Binding Priority}" FontWeight="Normal"></Label>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Grid Grid.Column="1" Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="107*"/>
                    <ColumnDefinition Width="91*"/>
                    <ColumnDefinition Width="198*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="60"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Content="Title" Grid.ColumnSpan="2"></Label>
                <Label Grid.Row="1" Content="Status" Grid.ColumnSpan="2"></Label>
                <Label Grid.Row="2" Content=" Priority" Grid.ColumnSpan="2"></Label>
                <Label Grid.Row="3" Content="Category" Grid.ColumnSpan="2"></Label>
                <Label Grid.Row="4" Content="Description" Grid.ColumnSpan="2"></Label>
                <Label Grid.Row="5" Content="CreationDate" Grid.ColumnSpan="2"></Label>

                <TextBox Grid.Row="0" Grid.Column = "2" Text="{Binding NewTask.Title, Mode=TwoWay}"></TextBox>
                <TextBox Grid.Row="1" Grid.Column = "2" Text="{Binding NewTask.Status, Mode=TwoWay}"></TextBox>
                <TextBox Grid.Row="2" Grid.Column = "2" Text="{Binding NewTask.Priority, Mode=TwoWay}"></TextBox>
                <TextBox Grid.Row="3" Grid.Column = "2" Text="{Binding NewTask.Category, Mode=TwoWay}"></TextBox>
                <TextBox Grid.Row="4" Grid.Column = "2" Text="{Binding NewTask.Description, Mode=TwoWay}"></TextBox>
                <TextBox Grid.Row="5" Grid.Column = "2" Text="{Binding NewTask.CreationDate, Mode=TwoWay}"></TextBox>
                <Button Grid.Row="6" Grid.Column="2"
                        Command="{Binding SaveNewTaskCommand}"
                        HorizontalAlignment="Right"
                        Margin="0,10,8,10"
                        ToolTip="Save"
                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                    <materialDesign:PackIcon Kind="Check"/>
                </Button>

            </Grid>
            <!--<materialDesign:Card Margin="0 4 4 0" Grid.Column="1" Grid.Row="2" Background="{DynamicResource PrimaryHueDarkBrush}"
                  Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                  Width="200"
                  Padding="8">
                <StackPanel>
                    <TextBlock Margin="16 16 12 8" FontSize="16">Notes</TextBlock>
                    <CheckBox Margin="16 4 16 0" Style="{StaticResource MaterialDesignUserForegroundCheckBox}">Do something good</CheckBox>
                    <CheckBox Margin="16 4 16 0" Style="{StaticResource MaterialDesignUserForegroundCheckBox}">Take a breather</CheckBox>
                    <CheckBox Margin="16 4 16 0" Style="{StaticResource MaterialDesignUserForegroundCheckBox}">And go create</CheckBox>
                    <Separator Style="{StaticResource MaterialDesignLightSeparator}"/>
                    <StackPanel Margin="8 0 8 8" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignToolForegroundButton}" 
                                Width="30" Padding="2 0 0 0"
                            materialDesign:RippleAssist.IsCentered="True">
                            <materialDesign:PackIcon Kind="CheckAll" />
                        </Button>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>-->
        </Grid>
    </DockPanel>
</Window>
